import { VercelRequest } from "@vercel/node";
import { VercelResponse } from "@vercel/node";
export class CorsHandler {
  setHeader(req: VercelRequest, res: VercelResponse) {
    res.setHeader;
  }
}
